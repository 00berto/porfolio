{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/" }],

  "scripts": {
    "build": "chmod +x ./node_modules/.bin/parcel && npm run build"
  }
}
